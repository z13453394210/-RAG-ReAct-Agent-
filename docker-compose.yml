version: "3.9"

services:
  app:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./vector_store/chroma_db:/app/vector_store/chroma_db
      - ./docs/uploads:/app/docs/uploads
    restart: unless-stopped
